<%= form_for(@subject2) do |f| %>
  <% if @subject2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subject2.errors.count, "error") %> prohibited this subject2 from being saved:</h2>

      <ul>
      <% @subject2.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
